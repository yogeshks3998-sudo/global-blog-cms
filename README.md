
  # Build complete app

  This is a code bundle for Build complete app. The original project is available at https://www.figma.com/design/XltZy5sifBiDmPMKmReDwU/Build-complete-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  